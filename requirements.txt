# Core LangChain packages used throughout the notebooks
langchain>=1.0.0
langchain-core>=1.0.0
langchain-classic>=1.0.0
langchain-community>=0.3.0
langchain-openai>=1.0.0
langchain-text-splitters>=1.0.0
langchain-chroma>=0.2.0

# LLM provider SDK
openai>=1.0.0

# Vector store backend used by langchain-chroma
chromadb>=0.5.0

# Document loaders (PyPDFLoader, Docx2txtLoader)
pypdf>=4.0.0
docx2txt>=0.8

# Numerical utilities
numpy>=1.26.0

# .env file support (used via `%load_ext dotenv` / `%dotenv` magics)
python-dotenv>=1.0.0

# Notebook runtime
jupyter>=1.0.0
ipykernel>=6.29.0
